https://gnats.netbsd.org/cgi-bin/query-pr-single.pl?number=49808

--- /home/cargos/pkgsrc-work/emulators/qemu/work/qemu-2.2.0/Makefile.orig
+++ /home/cargos/pkgsrc-work/emulators/qemu/work/qemu-2.2.0/Makefile
@@ -422,6 +424,7 @@
 	for d in $(TARGET_DIRS); do \
 	$(MAKE) $(SUBDIR_MAKEFLAGS) TARGET_DIR=$$d/ -C $$d $@ || exit 1 ; \
         done
+	touch $(DESTDIR)$(bindir)/qemu-tricore
 
 # various test targets
 test speed: all
